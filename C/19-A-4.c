#include<stdio.h>
int find(char ,char);
void main()
{
    char a[100],b[100];
    printf("Enter First String:");
    gets(a);
    printf("Enter Second String:");
    gets(b);
    int p=find(a,b)
    pr
}
int find(char a,char b)
{   int count=0;
    for(i=0;i!='\0';i++)
    {
        if(a[i]==b[i])
        {
            ;
        }
    }
}